#ifndef FUNCTIONS_H
#define FUNCTIONS_H

// Declarations do not require named parameters
// Though it is still a good practice especially if
// the parameters help indicate the purpose of the function
int factorial(int);

void hello(void);

double square(double);

int addition(int, int);

#endif